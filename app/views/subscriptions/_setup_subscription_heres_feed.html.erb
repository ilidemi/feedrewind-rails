<%= render partial: "common/info_popup" %>
<div class="flex flex-col gap-6">
  <div class="flex flex-col gap-1">
    <p>Here's your private feed link. Add it to your feed reader and you're all set!</p>
    <div>
      <%= render partial: "copy_feed_link",
                 locals: {
                   request: request,
                   subscription: subscription,
                   js_trigger_popup: "triggerInfoPopup"
                 }
      %>
    </div>
  </div>

  <% published_count = subscription.subscription_posts.where("published_at is not null").length %>
  <% if published_count == 1 %>
    <p>First entry has already arrived.</p>
  <% elsif published_count > 1 %>
    <p>First entries have already arrived.</p>
  <% else %>
    <%
      enabled_days_of_week = subscription
        .schedules
        .where("count > 0")
        .to_h { |schedule| [schedule.day_of_week, schedule.count] }
      first_schedule_date = ScheduleHelper.now.advance_till_midnight
      until enabled_days_of_week.include?(first_schedule_date.day_of_week)
        first_schedule_date = first_schedule_date.advance_till_midnight
      end

      next_enabled_day_of_week_str = first_schedule_date.date.strftime("%A, %B %-d")
    %>

    <% if enabled_days_of_week[first_schedule_date.day_of_week] == 1 %>
      <p>First entry will arrive on <%= next_enabled_day_of_week_str %> after midnight.</p>
    <% else %>
      <p>First entries will arrive on <%= next_enabled_day_of_week_str %> after midnight.</p>
    <% end %>
  <% end %>

  <p class="inline-flex flex-row gap-2">
    <%= link_to subscriptions_path, class: "btn-secondary bg-gray-50 inline-flex flex-row gap-1 items-center" do %>
      <!-- arrow-left icon -->
      <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3 mt-0.5 inline" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
      </svg>
      Dashboard
    <% end %>
    <%= link_to subscription_path, class: "btn-secondary bg-gray-50 inline-flex flex-row gap-0.5 items-center" do %>
      <!-- cog icon -->
      <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 mt-0.5 inline" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
      </svg>
      Manage
    <% end %>
  </p>
</div>