{
    let confirm_section = document.getElementById("confirm_section");
    confirm_section.innerHTML =
        "<%= j render partial: "looks_wrong",
                      locals: { subscription: @subscription }
         %>"
}