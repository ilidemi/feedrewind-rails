<div class="flex flex-col gap-6">
  <p>
    FeedRewind couldn't discover posts in this blog. We support over 80% of blogs, but unfortunately not this
    one.
  </p>
  <div class="inline-flex flex-row gap-2">
    <%= button_to "Try another",
                  SubscriptionsHelper::subscription_delete_path(subscription),
                  method: :post,
                  params: { redirect: :add },
                  class: "btn"
    %>
    <%= button_to "Discard",
                  SubscriptionsHelper::subscription_delete_path(subscription),
                  method: :post,
                  class: "btn"
    %>
  </div>
</div>